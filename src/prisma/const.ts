/* eslint-disable no-unused-vars */
export enum PrismaErrorCode {
  NotUnique = 'P2002', // Unique constraint failed on the constraint: `PRIMARY`
  RecordNotFound = 'P2025', // 'Record to update not found.'
}
